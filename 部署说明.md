# 部署到 GitHub Pages（给研究者）

医生打开一个网址就能做题，作答自动记进**你自己的 Google 表格**。

---

## 为什么要两样东西

GitHub Pages 是**纯静态托管**，只能发网页，没有数据库。所以：

| | 负责 |
|---|---|
| **GitHub Pages** | 放网页和图片，给医生一个网址 |
| **Google Apps Script + 表格** | 接收并保存作答记录 |

两样都免费，数据都在你自己账号里。

---

## 第 1 步：建收集端点（约 5 分钟，只做一次）

1. 打开 <https://sheets.google.com>，**新建一个空白表格**，名字随便（如「读片实验结果」）
2. 菜单 **扩展程序 → Apps Script**
3. 把编辑器里原有的 `function myFunction() {}` **全部删掉**
4. 打开本目录的 **`apps_script.gs`**，全文复制粘贴进去，**保存**（Ctrl/Cmd+S）
5. 右上角 **部署 → 新建部署**
   - 齿轮图标 → 选择类型 → **网页应用**
   - 说明：随便填
   - **执行身份：我**
   - **谁可以访问：任何人** ← 这一项必须选对，否则医生的浏览器发不进来
   - 点 **部署**，首次会要求授权，按提示允许
6. 复制得到的 **网页应用网址**（形如 `https://script.google.com/macros/s/AKfyc.../exec`）

**验证**：把这个网址直接粘到浏览器地址栏打开，看到 `OK reader-study endpoint alive` 就说明成了。

---

## 第 2 步：填进 config.js

打开本目录的 **`config.js`**，把网址粘进引号里：

```js
window.QUIZ_CONFIG = {
  SUBMIT_URL: "https://script.google.com/macros/s/AKfyc.../exec",
  AUTOSAVE_EVERY: 20
};
```

留空不填也能跑，只是不会上传，医生只能靠截图 / 导出 JSON 交回。

---

## 第 3 步：推到 GitHub

```bash
cd gh-pages
git init
git add -A
git commit -m "reader study"
git branch -M main
git remote add origin https://github.com/<你的用户名>/<仓库名>.git
git push -u origin main
```

然后在 GitHub 仓库页：**Settings → Pages → Source 选 `main` 分支 / `/ (root)` → Save**。

等一两分钟，网址就是：

```
第 1 轮  https://<你的用户名>.github.io/<仓库名>/
第 2 轮  https://<你的用户名>.github.io/<仓库名>/round2-si6yjj0o.html
```

> 仓库可以是 **Public**（免费用 Pages）。若必须 Private，GitHub Pages 需要付费方案。
> 整个目录约 65 MB，在 GitHub 的限制内（单文件 <100 MB、仓库建议 <1 GB）。

---

## 第 4 步：发给医生

**一开始**只发第 1 轮网址：
```
https://<你的用户名>.github.io/<仓库名>/
```

**两周后**再发第 2 轮网址（就是上面那个带随机后缀的）。

第 2 轮文件名故意起得猜不到，就是为了防止医生提前打开看 AI 提示。
**在你发出去之前不要公开这个链接。**

---

## 结果在哪看

回到第 1 步建的那张 Google 表格，会自动出现两个工作表：

**汇总** — 每位医生每轮一行

| 提交时间 | 用户名 | 姓名 | 单位 | 职称 | 轮次 | 已答 | 总题数 | 正确 | 正确率 | 净用时(分) | 校验码 | 状态 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|

**明细** — 每题一行，带医生的选择、正确答案、对错、耗时，以及该题的 **AI Top-1 / Top-5**，
可直接做「AI 对时是否被带对、AI 错时是否被带偏」的分析。

同一人同一轮重复提交（中途自动上传 + 最后提交）会**覆盖**旧行，不会堆重复。

---

## 医生那边是什么体验

1. 打开网址（不用注册、不用装东西）
2. 填 **用户名、姓名、单位、职称**（四项都必填，没有密码）
3. 做题：上方眼底图（可点开放大），下方 103 个选项
4. 每答 20 题自动上传一次，右上角有状态角标
5. 做完自动出结果页：正确率、用时、校验码 —— 可截图留底
6. 右上角可切换 **中文 / English**

⚠️ **务必告诉医生：第 2 轮要填与第 1 轮完全相同的用户名**，否则两轮记录对不上。

---

## 万一上传失败

网络不通或端点配错时，右上角角标会变红，结果页也会提示。这时数据**没有丢** ——
仍然存在医生浏览器里，让他们点「导出 JSON」下载文件发回来即可。

---

## 目录

```
gh-pages/
├── index.html                 第 1 轮（无 AI）
├── round2-si6yjj0o.html   第 2 轮（含 Top-5，顺序打乱）
├── config.js                  ← 只需改这个
├── app.js  app.css  i18n.js
├── data.js                    206 题 + 预先算好的 Top-5
├── images/                    206 张图
└── apps_script.gs             ← 贴进 Google Apps Script
```
